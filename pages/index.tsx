// features
import type { NextPage } from 'next';
import Head from 'next/head';
import { useRouter } from 'next/router';
// styles
import styles from '../styles/Home.module.css';

// main page
const Home: NextPage = () => {
  const router = useRouter();

  // navigation handler

  const routeHandler = (href: string): void => {
    router.push(href);
  };

  return (
    <div className={styles.container}>
      <Head>
        <title>Gorrion Summer Camp 2022 - recruitment task</title>
        <meta name='description' content='Generated by create next app' />
        <link rel='preconnect' href='https://fonts.googleapis.com' />
        <link
          rel='preconnect'
          href='https://fonts.gstatic.com'
          crossOrigin=''
        />
        <link
          href='https://fonts.googleapis.com/css2?family=Public+Sans:wght@500;600&display=swap'
          rel='stylesheet'
        ></link>
        <link rel='icon' href='/favicon.png' />
      </Head>

      <main className={styles.main}>
        <h1 className={styles.title}>
          <a href='https://gorrion.io'>Gorrion</a> Summer Camp 2022
        </h1>

        <p className={styles.description}>Let's check:</p>
        <section className={styles.buttons}>
          <button
            type='button'
            onClick={() => {
              routeHandler('/users');
            }}
          >
            FRONTEND
          </button>
          <button
            type='button'
            onClick={() => {
              routeHandler('/api/users');
            }}
          >
            BACKEND
          </button>
        </section>
      </main>
    </div>
  );
};

export default Home;
